@model Rss.Server.Models.Folder

@{
    ViewBag.Title = "Folder";
}


<h2>@Model.Name</h2>

@{
    foreach (var feed in Model.Feeds)
    {
    <div>
        <h3><a href="\home\feed\@feed.Id">@feed.Name</a></h3>
    </div>
    }
}

